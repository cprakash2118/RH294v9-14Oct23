---
- name: synchronize example 
  hosts: dev 
  tasks:
   - name: Synchronization of src on the control machine to dest on the remote hosts
     ansible.posix.synchronize:
       src: /home/student/New-Work/
       dest: /root/
 
